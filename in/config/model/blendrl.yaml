# BlendRL framework — composes sub-models with learned arbitration
actor_mode: hybrid
neural_actor: dnn
symbolic_actor: nsfr
blender:
  mode: neural
  blend_function: softmax
modules:
  - type: nsfr
    rules: default
  - type: neural
