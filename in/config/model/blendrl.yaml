# BlendRL framework — composes symbolic and neural policy modules
actor_mode: hybrid
blender:
  mode: neural
  blend_function: softmax
blend_q_values: true


neural:
  type: dnn
  hidden_sizes: [256, 256]

symbolic:
  type: nsfr
  rules: default
