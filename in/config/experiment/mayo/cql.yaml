# @package _global_
defaults:
  - mayo/_base

methods:
  cql_dnn:
    agent: cql
    model: dnn
    epochs_per_interval: 25
    eval_interval_epochs: 5
    batch_size: 1024
    lr: 2e-4
    cql_alpha: 2.0
    reward_scale: 0.1
    bellman_loss: smooth_l1
    weight_decay: 1e-4
    use_actor: false
