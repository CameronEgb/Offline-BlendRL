# @package _global_
defaults:
  - pyrenees/_base

methods:
  cql_dnn:
    agent: cql
    model: dnn
    epochs_per_interval: 50
    eval_interval_epochs: 5
    batch_size: 256
    lr: 3e-4
    cql_alpha: 1.0
    soft_target_tau: 0.005
