# @package _global_
defaults:
  - tuning/_base
  - override /env: cartpole
  - override /hydra/sweeper: optuna_offline

paradigm: offline_rl
total_timesteps: 250000
intervals_count: 1
eval_episodes: 100

dataset_path: in/datasets/ungrouped/cp_final/ppo_cp_tuned

trainer:
  limit_train_batches: 500

methods:
  cew_dnn:
    agent: cew
    model: dnn

hydra:
  sweeper:
    sampler:
      _target_: optuna.samplers.TPESampler
      seed: 123
    _target_: hydra_plugins.hydra_optuna_sweeper.optuna_sweeper.OptunaSweeper
    direction: maximize
    storage: "sqlite:///results/optuna/optuna.db"
    study_name: "tune_cew_${agent.name}"
    n_trials: 15
    n_jobs: 1
    params:
      agent.epochs_per_interval: 20
      agent.ecm_dthr: interval(0.01, 0.5)
      agent.cql_alpha: interval(0.1, 10.0)
      agent.eps: interval(0.1, 0.9)
      agent.lr: choice(1e-4, 3e-4, 1e-3)
      agent.batch_size: choice(32, 64)
