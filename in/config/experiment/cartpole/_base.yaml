# @package _global_
defaults:
  - override /env: cartpole

paradigm: online_rl

save_dataset: false
seed: 1
recover: false

plots:
  losses:
    metrics: ["losses/total_loss", "losses/policy_loss", "losses/value_loss", "losses/entropy", "losses/blend_entropy"]
  convergence: {}
