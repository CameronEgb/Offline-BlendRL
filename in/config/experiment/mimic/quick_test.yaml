# @package _global_
defaults:
- mimic/_base
no_plot: true

trainer:
  limit_train_batches: 2
  limit_val_batches: 2
methods:
  cql_dnn:
    agent: cql
    model: dnn
    batch_size: 256
    epochs_per_interval: 1
    eval_interval_epochs: 1
    lr: 1e-4
    cql_alpha: 2.0
    weight_decay: 1e-4
